#include <stdio.h> // Biblioteca com funções de entrada e saída de dados

/*
Esta é a função principal (main) do programa.
Ela é o ponto de entrada da execução.
*/
int main() { 

    // A função printf imprime a mensagem "Hello, World!"
    printf("Hello, World!\n"); // \n é um caractere de escape que representa uma quebra de linha.

    return 0; // A função main deve retornar um valor inteiro (geralmente 0 para indicar sucesso).
}
